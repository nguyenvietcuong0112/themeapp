package com.starnest.upsale.ui.view;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.FrameLayout;

public class UpsalePopupExitView extends FrameLayout {
    public UpsalePopupExitView(Context context) {
        super(context);
    }
    public UpsalePopupExitView(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
    public UpsalePopupExitView(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
